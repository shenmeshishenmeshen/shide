@model IEnumerable<Wedding.Models.LeiBie>

@foreach (var item in Model)
{
    <div class="content_bottom">
        <div class="heading">
            <h3><b>@item.Name</b></h3>
        </div>

        <div class="clear"></div>
    </div>
    <div class="section group">
        @{Html.RenderAction("AllProuduct", "Home",new {id=item.LeiBieId });}
    </div>
    <div class="see">
        <p><a href="@Url.Action("List","ShangPing19man",new { id=item.LeiBieId})">更多>></a></p>
    </div>
    <div class="clear"></div>
}
