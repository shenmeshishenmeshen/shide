@model IEnumerable<Wedding.Models.Prouduct>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("新增", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LeiBie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Varieties)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsNew)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TuPian)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LeiBie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Varieties)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsNew)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TuPian)
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id=item.ProuductId }) |
            @Html.ActionLink("详情", "Details", new { id=item.ProuductId }) |
            @Html.ActionLink("删除", "Delete", new { id=item.ProuductId })
        </td>
    </tr>
}

</table>
